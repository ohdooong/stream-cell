Config:
    name: Log4j2Config
    Properties:
        Property:
            name: LOG_LEVEL_PATTERN
            value: "{%5p}{FATAL=red blink, ERROR=red bold, WARN=yellow bold, INFO=green bold, DEBUG=blue bold, TRACE=blue}"
    Appenders:
        Console:
            name: CONSOLE
            target: SYSTEM_OUT
            PatternLayout:
                disableAnsi: false
                pattern: "%style{%d{yyyy-MM-dd HH:mm:ss.SSS}}{magenta} %highlight${LOG_LEVEL_PATTERN} %style{[%c{1.1.1.*}]}{cyan} %m%n"

        RollingFile:
            name: FILE
            fileName: "logs/application.log"
            filePattern: "logs/application-%d{yyyy-MM-dd}-%i.log.gz"
            PatternLayout:
                pattern: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"
            Policies:
                SizeBasedTriggeringPolicy:
                    size: "10MB"
            DefaultRolloverStrategy:
                max: "30"

    Loggers:
        Root:
            level: INFO
            AppenderRef:
                - ref: CONSOLE
                - ref: FILE
        Logger:
            # 애플리케이션 로그
            - name: com.streamcell
              level: DEBUG
              additivity: false
              AppenderRef:
                  - ref: CONSOLE
                  - ref: FILE
            - name: org.springframework
              level: INFO
            - name: jdbc.sqlonly
              level: "OFF"
            - name: jdbc.audit
              level: "OFF"
            - name: com.zaxxer.hikari
              level: INFO
